# CloudFlare Pages redirects
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
# This file lives in static/ so Hugo copies it verbatim into public/_redirects
# at build time. It replaces the [[redirects]] blocks that used to be in
# wrangler.toml — CloudFlare Pages' wrangler.toml (BETA) support does not
# accept a top-level "redirects" field at all (confirmed by a real deploy
# failure); _redirects is the actual supported mechanism for Pages projects.

/discord  https://discord.gg/RtG4nyCEDX  301
/github   https://github.com/DXersCommunity/dxers-site  301
